// Tic-Tac-Toe — vanilla JavaScript
// Day 1: scaffold only. Game logic arrives in later steps.

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  const status = document.getElementById("status");
  if (status) {
    status.textContent = "Ready — game logic coming soon";
  }
});
